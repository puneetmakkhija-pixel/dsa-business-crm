# Supabase (public, read-only — safe to expose; tables have RLS select-only policies)
# The app falls back to these same values in lib/supabase.ts if unset.
NEXT_PUBLIC_SUPABASE_URL=https://ymdkcaedwnnhszhzirli.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_zEpxif_DYwn7UiBW6FP9CA_IEDSIAfN

# Set true to show the demo role-switcher (disable in real production)
NEXT_PUBLIC_ENABLE_ROLE_SWITCHER=true
